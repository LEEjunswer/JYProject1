<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.JYProject.project.repository.mybatis.MemberMapperRepository">
    <resultMap id="memberResultMap" type="com.JYProject.project.model.dto.MemberDTO">
        <id column="member_id" property="memberId" />
        <result column="login_id" property="loginId" />
        <result column="pw" property="pw" />
        <result column="name" property="name" />
        <result column="nickname" property="nickname" />
        <result column="phone" property="phone" />
        <result column="email" property="email" />
        <result column="zip_code" property="zipCode" />
        <result column="address" property="address" />
        <result column="address_detail" property="addressDetail" />
        <result column="reg_date" property="regDate" />
        <result column="last_login_date" property="lastLoginDate" />
        <result column="active" property="active" />
        <result column="grade" property="Grade" />
        <result column="point" property="point" />
    </resultMap>

<<<<<<< HEAD
    <insert id="insertMember" parameterType="com.JYProject.project.model.dto.MemberDTO">
        insert into member (login_id,pw,name,nickname,phone,email,zip_code,address,address_detail)
        values (#{loginId},#{pw},#{name},#{nickname},#{phone},#{email},#{zipCode},#{address},#{addressDetail})
    </insert>

    <select id="selectMemberDetail" parameterType="String" resultMap="memberResultMap">
        select * from member where login_id=#{loginId}
=======
    <insert id="insertMember" parameterType="MemberDTO">
        insert into member (loginid, pw, name, nickname, phone, email, zipcode, address, address_detail, member_profile,
                            last_login_date)
        values (#{loginId}, #{pw}, #{name}, #{nickname}, #{phone}, #{email}, #{zipcode}, #{address}, #{addressDetail},
                #{profilePic}, #{last_login_date})
    </insert>

    <select id="selectMemberDetail" parameterType="Long" resultType="com.JYProject.project.model.dto.MemberDTO">
        select *
        from member
        where id = #{id}
>>>>>>> main
    </select>

    <update id="updateMember" parameterType="com.JYProject.project.model.dto.MemberDTO">
        update member
<<<<<<< HEAD
        set    <if test="pw != null and pw != ''">
        pw = #{pw},
    </if>
            nickname=#{nickname},
            phone = #{phone},
            email = #{email},
            zip_code = #{zipCode},
            address = #{address},
            address_detail = #{addressDetail}
        where member_id=#{memberId};
    </update>
    <delete id="deleteMember" parameterType="Long" >
        delete from member where member_id=#{Id}
    </delete>

    <select id="MemberAllList" resultMap="memberResultMap">
        select *from member
=======
        set pw=#{pw},
            nickname=#{nickname},
            phone = #{phone},
            email = #{email},
            zipcode = #{zipcode},
            address = #{address},
            address_detail = #{addressDetail},
            member_profile =#{profilePic}
        where id = #{id};
    </update>
    <delete id="deleteMember" parameterType="Long">
        delete
        from member
        where id = #{id}
    </delete>

    <select id="MemberAllList" resultType="com.JYProject.project.model.dto.MemberDTO">
        select *
        from member
>>>>>>> main
    </select>
    <select id="selectMemberTotalCount" resultType="Integer">
        select count(*)
        from member;
    </select>
<<<<<<< HEAD
    <select id="validCheckId" resultType="boolean" parameterType="String">
        SELECT COUNT(*) > 0 FROM member WHERE login_id=#{loginId} AND pw=#{pw}
    </select>
    <select id="login"  parameterType="com.JYProject.project.model.dto.MemberDTO" resultMap="memberResultMap">
        select * from member where login_id=#{loginId} and pw=#{pw};
    </select>

    <select id="checkIdAndPw"  resultType="boolean" parameterType="com.JYProject.project.model.dto.MemberDTO">
        SELECT COUNT(*) > 0 FROM member WHERE login_id=#{loginId} AND pw=#{pw}
=======
    <select id="validCheckId" resultType="Boolean" parameterType="String">
        select count(*)
        from member
        where loginid = ${loginId};
    </select>
    <select id="loginId" resultType="com.JYProject.project.model.dto.MemberDTO"
            parameterType="com.JYProject.project.model.dto.MemberDTO">
        select *
        from member
        where loginid = #{loginId}
          and pw = #{pw};
    </select>

    <select id="checkIdAndPw" resultType="Boolean" parameterType="com.JYProject.project.model.dto.MemberDTO">
        select *
        from member
        where loginid = #{loginId}
          and pw = #{pw};
>>>>>>> main
    </select>

</mapper>